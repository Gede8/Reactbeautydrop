need nvm install
expo-router not the cli
node_module package lock by running npm install
back end needs stripe
needs babel install
need stacknavigation install
the assets and components could be downloaded
requires expo to run
